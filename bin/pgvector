#!/bin/bash

set -e

docker volume create pgvector-data

docker run --name pgvector -v pgvectordata:/var/lib/postgresql/data -e POSTGRES_PASSWORD=password -e POSTGRES_USER=forge --publish 5432:5432 -it -d pgvector/pgvector:pg17